%!TEX root = ../main.tex
\chapter{検証データ}\label{chap:dataset}
\section{合成データ}\label{sec:simulated-dataset}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.70\textwidth]{figure/60_data/dataset-capturing.png}
  \caption{
    データ取得環境の概要。
    直下視(Nadir)に加え、2種類の斜め視(Oblique)軌道から対象の地点を撮影した。
    }
    \label{fig:dataset-capture}
\end{figure}

% \note{
%   * 実際の取得画像を示すべき
%   * 新たなBlenderデータセットを完成させ、10種類ほどの多様なデータセットで複合的な検証を行いたかったが、、
% }

本節では、本実験で使用したCGシミュレーションによる合成データセットの詳細について述べる。
データ取得環境の概要を \cref{fig:dataset-capture} に示す。
実験環境の構築にあたり、本研究の主目的である「屈折による幾何学的歪み」の影響を厳密に検証するため、3DCGソフトウェアBlender\cite{blender}を用いたシミュレーション環境を採用した。
これにより、実環境の実画像において不可避である水面反射や水中での光の減衰・散乱の影響を排除し、純粋な屈折現象のみに焦点を当てた評価を可能とした。
河床のシーン構築は、礫のテクスチャに対してDisplacementモディファイアを適用することで、大きな凹凸を持つ地形モデルを作成した。
レンダリングにはパストレーシングエンジンであるBlender Cyclesを使用し、物理的に忠実な画像を生成した。
水面モデルには歪みのない理想的な平面を仮定した。
撮影は、通常の写真測量でしばし用いられる直下視(ナディア; Nadir)による観測に加え、地形モデルの中心に対して天頂角20°および40°で対象を取り囲むように円軌道から撮影する条件を採用した(オブリーク; Oblique)。
ナディア撮影ではオーバーラップ率およびサイドラップ率を共に85\%で、計25枚の画像を出力した。
オブリーク撮影では、軌道ごとに中心から10°旋回しながら計72枚の画像を出力した。
地形の平均水深は\qty{10}{m}であり、水面からのカメラ高度も\qty{10}{m}と、深度に対する相対高度が1:1となる非常に屈折の影響を受けやすい撮影条件である。
このような多視点からの観測は、三次元復元における幾何学的制約を強める一方で、\cref{fig:space_compression_by_view} に示すように、浅い角度での観測に伴う大きな屈折歪みや見かけ位置の変動を導入する要因ともなる。
センサモデルには歪みのないピンホールカメラモデルを採用し、Field of View(FOV)は70°、画像解像度は$800 \times 800$ピクセルとした。
Gaussian Splattingのトレーニングに用いるデータセットの総枚数は、10枚ずつ抜いた新規視点合成の評価用画像10枚を除き、計87枚である。
\cref{fig:simulated-dataset}に合成データの画像例を示す。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{figure/60_data/simulated.png}
  \caption{
    シミュレーションデータセットの画像例。\\
    (左) 屈折なしでレンダリングした画像。\\
    (右) 同じカメラパラメータで屈折をシミュレートしてレンダリングした画像。
    こちらをRA-GSの入力として用いる。
    }
    \label{fig:simulated-dataset}
\end{figure}